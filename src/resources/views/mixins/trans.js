export const trans = {
    mounted(){
        // translations
        this.trans = {
            __true: this.__('base.booleans.true'),
            __false: this.__('base.booleans.false')
        };
    }
};